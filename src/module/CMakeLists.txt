add_subdirectory (ping)
IF(ENABLE_KOKO)
    add_subdirectory (koko)
ENDIF(ENABLE_KOKO)
add_subdirectory (smart)
add_subdirectory (pluginexecutor)
add_subdirectory (history)
add_subdirectory (help)
IF (ENABLE_WHO)
    add_subdirectory (who)
ENDIF (ENABLE_WHO)
