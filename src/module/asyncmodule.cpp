#include <string>
#include "../debug/debug.h"
std::string generate_answer(const std::string& sender,
                            const std::string& text)
    {
        return std::string("Async module called");
    }
